---
import PlanCard from '../components/PlanCard.astro';
import { getCollection } from 'astro:content';

const plans = (await getCollection('blog')).sort(
	(a, b) => a.data.date.valueOf() - b.data.date.valueOf()
);
---

<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>珍スポ研究室</title>
    <style>
        html, body {
            margin: 0;
            padding: 0;
        }
        p {
            margin: 0;
        }
        body {
            font-family: "Helvetica Neue", "Helvetica", "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Arial", "Yu Gothic", "Meiryo", sans-serif;
            background: url("/images/chinspot-back.jpg") no-repeat center center fixed;
            animation: float 0.14s ease-in-out .4s 3;
            background-size: auto calc(100% + 20px);
            background-color: black;
        }
        @media (min-width: 480px) {
             body {
                background-size: cover;
                animation: none;
            }
        }
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: -1;
        }
        #greeting {
            padding: 12px;
            margin: 0 auto;
        }
        #greeting h1 {
            color: lime;
            font-size: 2.4rem;
            text-align: center;
        }
        #greeting .description {
            background-color: rgba(0, 0, 0, 0.4);
            color: #ffffff;
            padding: 12px;
        }
        .sns-list {
          display: flex;
          flex-direction: column;
          width: 100%;
          margin: 0 auto;
          gap: 24px;
        }

        @media (min-width: 1076px) {
          .sns-list {
            flex-direction: row;
          }
        }
        .sns-button {
            display: flex;
            max-width: 100%;
            padding: 12px 16px;
            background: linear-gradient(90deg, #10CAC3 0%, #07C662 100%);
            color: #ffffff;
            text-decoration: none;
            border-radius: 12px;
            align-items: center;
        }
        @media (min-width: 1076px) {
          .sns-button {
            width: 526px;
          }
        }
        .sns-button img {
          margin-right: 12px;
          border-radius: 50%;
        }
        .sns-title {
          font-weight: bold;
        }
        section h2 {
            color: lime;
        }
        .events-section {
            padding: 4px;
            margin: 0 auto;
        }
        @media (min-width: 320px) {
          .events-section {
            padding: 12px;
          }
        }
        @media (min-width: 424px) {
          #greeting {
            width: 400px;
          }
          .events-section {
            width: 400px;
          }
        }
        @media (min-width: 720px) {
           /* 720 = 696 + 12 * 2 */
          #greeting {
            width: 696px;
          }
          .events-section {
            padding: 12px;
            width: 696px; /* 696 = 340 * 2 + 16 */
            margin: 0 auto;
          }
        }
        @media (min-width: 1076px) {
           /* 1076 = 1052 + 12 * 2 */
          #greeting {
            width: 1052px;
          }
          .events-section {
            padding: 12px;
            width: 1052px; /* 1052 = 340 * 3 + 16 * 2 */
            margin: 0 auto;
          }
        }
        .event-list {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
        }
        .empty-events {
            color: #ffffff;
            font-size: 2rem;
        }
@keyframes float {
  0% {
    background-position: center;
  }
  50% {
    background-position: center -20px;
  }
  100% {
    background-position: center;
  }
}
    </style>
</head>
<body>
    <div class="overlay"></div>
<section id="greeting">
    <h1>珍スポ研究室</h1>
    <div class="description">
       <p>珍スポ研究室とは、珍スポット（B級スポット）が好きな人たちが集まるコミュニティです。</p>
       <p>主にLINEオープンチャットで活動しています。</p>
    </div>
</section>
<section id="sns" class="events-section">
  <h2>SNS</h1>
  <div class="sns-list">
    <a class="sns-button" href="https://line.me/ti/g2/o6s0F62sSXO5k_dkl6xukS_pzeEnu6_5WAAJ2w">
      <img src="/images/chinspot-kenkyu.png" width="48" height="48" alt="珍スポ研究室" />
      <div>
        <div class="sns-title">珍スポ研究室</div>
        <p>珍スポの情報を共有するLINEオープンチャットです。</p>
        <p>こちらがメインの活動場所になります。</p>
      </div>
    </a>
    <a class="sns-button" href="https://line.me/ti/g2/PZfs6bwr4EejequqSQYxW5Xc3zFotxYJqI3S0w">
      <img src="/images/chinspot-tanken.png" width="48" height="48" alt="珍スポ探検隊" />
      <div>
        <div class="sns-title">珍スポ探検隊</div>
        <p>オフ会用LINEオープンチャットです。</p>
        <p>オフ会参加希望の方はお入りください。</p>
      </div>
    </a>
  </div>
</section>
<section id="future-events" class="events-section">
    <h2>これからのイベント</h1>
    <div class="event-list">
      {
			  plans.map((plan) => (
          <PlanCard plan={{ key: plan.slug, ...plan.data}} />
        ))
      }
    </div>
</section>
<section id="passed-events" class="events-section">
    <h2>過去のイベント</h1>
    <div class="empty-events">工事中...</div>
</section>
</body>
</html>
